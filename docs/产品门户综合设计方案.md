# 产品门户综合设计方案

**版本**: v1.0
**创建日期**: 2026-01-31
**基于文档**: 产品门户规划方案.md + PORTAL_IMPLEMENTATION_PLAN.md

---

## 一、项目概述

### 1.1 项目背景

基于现有"产品能力匹配系统",构建面向客户的产品介绍门户,将内部管理工具升级为对外服务门户,实现产品能力展示、智能匹配、解决方案推荐等功能。

### 1.2 目标用户

- **外部客户**: 了解产品功能、技术参数、应用场景,快速找到满足需求的产品
- **销售团队**: 快速匹配客户需求,生成对比报告,提升销售效率
- **产品管理者**: 查看产品能力覆盖度,了解市场反馈
- **市场团队**: 展示解决方案、客户案例,进行产品营销

### 1.3 核心目标

1. **提升用户体验**: 提供直观、友好的产品展示和智能匹配服务
2. **降低使用门槛**: 无需登录即可使用核心功能,简化操作流程
3. **增强营销能力**: 通过解决方案、案例研究等功能提升产品曝光度
4. **数据驱动决策**: 收集用户行为数据,指导产品优化和市场策略

---

## 二、系统架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    产品门户 (Frontend - Vue 3)                │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐  │
│  │  门户首页    │  │  产品中心    │  │   智能匹配      │  │
│  │  (Home)     │  │  (Products) │  │   (Matching)    │  │
│  └──────────────┘  └──────────────┘  └─────────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐  │
│  │  解决方案    │  │  资源中心    │  │   关于我们      │  │
│  │ (Solutions) │  │ (Resources) │  │    (About)      │  │
│  └──────────────┘  └──────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────┐
│              API网关层 (Django REST Framework)              │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐  │
│  │ 门户产品API │  │ 匹配服务API  │  │   资源下载API   │  │
│  │ (Portal)    │  │ (Matching)  │  │   (Download)    │  │
│  └──────────────┘  └──────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────┐
│              业务逻辑层 (Django Apps)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐  │
│  │ 产品管理    │  │ 功能特性    │  │   匹配算法      │  │
│  │ (Products)  │  │ (Features)  │  │   (Matching)    │  │
│  └──────────────┘  └──────────────┘  └─────────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐  │
│  │ 解决方案    │  │ 资源管理    │  │   用户行为      │  │
│  │ (Solutions) │  │ (Resources) │  │   (Analytics)    │  │
│  └──────────────┘  └──────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────┐
│              数据层 (PostgreSQL + pgvector + Redis)          │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐  │
│  │ 产品数据    │  │ 向量索引    │  │   缓存层        │  │
│  │ (Products)  │  │ (pgvector)  │  │   (Redis)       │  │
│  └──────────────┘  └──────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

#### 前端技术栈

| 类别 | 技术选型 | 用途 |
|------|---------|------|
| 框架 | Vue 3 | 核心框架 |
| 语言 | TypeScript | 类型安全 |
| UI库 | Element Plus | 组件库 |
| 状态管理 | Pinia | 状态管理 |
| 路由 | Vue Router 4 | 路由管理 |
| HTTP客户端 | Axios | API请求 |
| 构建工具 | Vite | 构建打包 |
| 可视化 | ECharts + vue-echarts | 图表展示 |
| 轮播 | Swiper | 轮播组件 |
| PDF导出 | jsPDF + html2canvas | PDF生成 |
| 文件处理 | xlsx, mammoth | 文件解析 |

#### 后端技术栈

| 类别 | 技术选型 | 用途 |
|------|---------|------|
| 框架 | Django 4.2 + DRF | Web框架 |
| 数据库 | PostgreSQL 12+ | 主数据库 |
| 向量搜索 | pgvector | 向量相似度搜索 |
| 缓存 | Redis 6+ | 缓存和队列 |
| 异步任务 | Celery | 异步处理 |
| AI模型 | OpenAI API, Sentence-Transformers | Embedding生成 |
| API文档 | drf-spectacular | OpenAPI文档 |

---

## 三、功能模块设计

### 3.1 门户首页 (Portal Home)

**路径**: `/`

#### 功能点

**1. Hero区域**
- 标题: "智能安全产品能力匹配平台"
- 副标题: "基于AI语义分析,快速找到满足需求的产品"
- 价值主张标签: "精准匹配 · 智能推荐 · 高效决策"
- 快速匹配入口:
  - 文本需求输入框
  - 文件上传按钮
  - "立即匹配"CTA按钮
- 快速导航按钮: "探索产品"、"查看解决方案"

**2. 产品分类导航**
- 分类展示方式: 图标+名称的网格布局
- 分类维度:
  - 资产测绘与攻击面管理
  - 互联网暴露面测绘运营
  - 安全大数据平台
  - 安全管理和自动化编排
  - 综合安全平台
- 动态数据: 从数据库读取实际产品分类

**3. 热门产品推荐**
- 展示数量: 6-8个产品
- 筛选逻辑: `is_featured=True` + 高浏览量
- 卡片内容:
  - 产品名称(大字体)
  - 厂商信息
  - 子系统类型标签(彩色)
  - 产品描述(2行截断)
  - 核心功能预览(3个标签)
  - "查看详情"按钮

**4. 解决方案轮播**
- 展示数量: 4-6个解决方案
- 轮播组件: Swiper自动轮播
- 卡片内容:
  - 解决方案名称
  - 适用行业/场景
  - 核心价值
  - 关联产品数量

**5. 快速统计**
- 产品总数
- 功能特性总数
- 客户案例数
- 解决方案数量

**6. 使用指南时间线**
- 步骤1: 选择产品分类
- 步骤2: 输入需求或上传文件
- 步骤3: 查看匹配结果
- 步骤4: 导出报告或联系销售

#### 页面布局

```
┌─────────────────────────────────────────────────┐
│  [Logo] [产品] [解决方案] [资源] [关于] [登录]  │
├─────────────────────────────────────────────────┤
│                                                 │
│  智能安全产品能力匹配平台                        │
│  基于AI语义分析,快速找到满足需求的产品           │
│  精准匹配 · 智能推荐 · 高效决策                  │
│                                                 │
│  [输入需求...] [上传文件] [立即匹配]            │
│  [探索产品] [查看解决方案]                      │
│                                                 │
├─────────────────────────────────────────────────┤
│  产品分类                                       │
│  [资产测绘] [暴露面] [大数据] [SOAR] [综合]     │
├─────────────────────────────────────────────────┤
│  热门产品                                       │
│  [产品卡片1] [产品卡片2] [产品卡片3]            │
│  [产品卡片4] [产品卡片5] [产品卡片6]            │
├─────────────────────────────────────────────────┤
│  解决方案 (轮播)                                │
│  << [解决方案卡片] >>                           │
├─────────────────────────────────────────────────┤
│  使用指南                                       │
│  步骤1 → 步骤2 → 步骤3 → 步骤4                  │
└─────────────────────────────────────────────────┘
```

---

### 3.2 产品中心 (Product Center)

**路径**: `/products`

#### 功能点

**1. 筛选面板(左侧,可折叠)**
- 子系统类型(多选):
  - asset_mapping
  - exposure_mapping
  - big_data
  - soar
  - 综合安全平台
- 产品分类(多选)
- 厂商(多选)
- 指标类型(多选):
  - product_function
  - performance
  - security
  - reliability
  - usability
- 重要性等级(滑块: 1-10)

**2. 搜索栏**
- 关键词搜索(产品名称、描述、功能)
- 实时搜索建议(防抖300ms)
- 搜索历史记录

**3. 视图切换**
- 卡片视图(默认,3列响应式)
- 列表视图(详细信息)

**4. 排序选项**
- 最新发布
- 名称(A-Z)
- 功能数量
- 浏览量
- 自定义权重

**5. 分页加载**
- 默认每页20条
- 支持分页器或无限滚动
- 虚拟滚动优化(超过50个产品)

#### 产品卡片设计

```
┌─────────────────────────┐
│ [图标] 产品名称 v2.0     │
│        厂商: XX科技      │
├─────────────────────────┤
│ 产品描述...              │
│                         │
│ 标签: [功能1][功能2]    │
│ 子系统: 资产测绘         │
├─────────────────────────┤
│ 功能数: 128  浏览: 45    │
│ [查看详情] [加入对比]    │
└─────────────────────────┘
```

---

### 3.3 产品详情页 (Product Detail)

**路径**: `/products/:id`

#### 功能点

**1. 产品头部信息**
- 产品名称(大标题)
- 版本号
- 厂商(带链接)
- 子系统类型(彩色标签)
- 产品分类
- 更新时间

**2. 产品概述**
- 产品描述(富文本)
- 核心价值点(图标+文字,JSON存储)

**3. 技术参数元数据**
- 性能指标
- 系统要求
- 部署方式
- 许可证类型
- 支持服务

**4. 功能特性列表(树形结构)**
- 一级功能(可折叠)
  - 二级功能
    - 三级功能
      - 功能名称
      - 描述
      - 指标类型(标签)
      - 重要性等级(1-10星)
- 功能搜索(高亮匹配项)
- 功能统计(总数、分布)

**5. 功能统计可视化**
- 各指标类型分布(饼图)
- 重要性等级分布(柱状图)

**6. 相关产品推荐**
- 同子系统类型产品
- 同厂商其他产品
- 功能相似产品(基于向量相似度)

**7. 操作按钮**
- "加入对比"
- "下载资料"
- "申请试用"
- "联系销售"

---

### 3.4 产品对比 (Product Compare)

**路径**: `/compare`

#### 功能点

**1. 产品选择器**
- 最多选择4个产品
- 搜索添加产品
- 已选产品标签(可删除)

**2. 对比维度选择**
- 基本信息
- 功能特性
- 技术参数
- 子系统类型

**3. 对比表格**
- 行:对比项
- 列:产品(2-4列)
- 差异高亮(不同值标红)
- 相同值合并显示

**4. 功能对比详情**
- 共同功能(交集)
- 独有功能(差集)
- 功能覆盖度(百分比)

**5. 可视化对比**
- 雷达图(多维度评分)
- 柱状图(功能数量对比)
- 词云(功能关键词)

**6. 导出功能**
- 导出PDF报告
- 导出Excel表格
- 分享对比链接(生成临时链接)

#### 对比表格设计

```
┌──────────┬──────────┬──────────┬──────────┐
│ 对比项    │ 产品A    │ 产品B    │ 产品C    │
├──────────┼──────────┼──────────┼──────────┤
│ 厂商      │ XX科技   │ YY安全   │ ZZ防护   │
│ 版本      │ v2.0     │ v1.5     │ v3.0     │
│ 功能覆盖度│ 95%      │ 87%      │ 92%      │
│ 功能数    │ 128      │ 96       │ 115      │
│ 独有功能  │ 15       │ 3        │ 8        │
└──────────┴──────────┴──────────┴──────────┘
```

---

### 3.5 智能匹配中心 (Matching Center)

**路径**: `/matching`

#### 功能点

**1. 需求输入页**

**文本输入**:
- 多行文本输入框
- 支持批量输入(每行一个需求)
- 实时字数统计
- 需求模板加载

**文件上传**:
- 支持Excel (.xlsx, .xls)
- 支持CSV (.csv)
- 支持Word (.docx)
- 拖拽上传区域
- 文件大小限制(10MB)
- 文件类型验证

**需求模板**:
- 内置模板下载
  - 标准需求模板(Excel)
  - 功能清单模板(CSV)
  - 技术规格模板(Word)

**示例需求**:
- 加载示例数据
- 帮助用户理解输入格式
- 快速体验匹配功能

**高级选项**:
- 匹配阈值设置(0.75-0.95,默认0.85)
- 选择Embedding模型
- 匹配范围选择(全部产品/指定类别)

#### 2. 匹配结果页

**匹配结果可视化**:
- 饼图: 满足项/部分满足项/不满足项占比
- 柱状图: 各产品匹配度对比
- 雷达图: 多维度能力覆盖评估

**产品匹配度排序列表**:
- 按匹配度降序排列
- 显示匹配度百分比
- 显示满足项/部分满足项/不满足项数量

**分类展示**:
- 满足项(≥0.85): 绿色标签,显示匹配的功能特性
- 部分满足项(0.75-0.85): 黄色标签,显示相似功能
- 不满足项(<0.75): 红色标签,显示缺失能力

**详细匹配报告**:
- 每个需求的匹配详情
- 相似功能推荐
- 替代方案建议

**报告导出**:
- PDF导出: 完整匹配报告,包含图表和详细数据
- Excel导出: 结构化数据,便于进一步分析
- 分享链接: 生成临时链接,方便分享结果

#### 可视化设计

```
┌─────────────────────────────────────────┐
│ 匹配结果总览                           │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │ 满足项  │  │ 部分满足│  │ 不满足  │ │
│  │   65%   │  │   25%   │  │   10%   │ │
│  └─────────┘  └─────────┘  └─────────┘ │
├─────────────────────────────────────────┤
│ 产品匹配度排名                          │
│ 1. 产品A  92%  ████████████████████░   │
│ 2. 产品B  87%  ██████████████████░░░   │
│ 3. 产品C  78%  ███████████████░░░░░   │
├─────────────────────────────────────────┤
│ 详细匹配情况                            │
│ 需求1: 用户认证                        │
│   └─ 产品A: ✓ 完全满足 (相似度0.95)    │
│   └─ 产品B: ~ 部分满足 (相似度0.82)    │
└─────────────────────────────────────────┘
```

---

### 3.6 解决方案中心 (Solutions)

**路径**: `/solutions`

#### 功能点

**解决方案分类**:
- 按行业: 金融、政府、企业、医疗、教育
- 按场景: 安全运营、威胁检测、合规审计

**解决方案卡片**:
- 解决方案名称
- 适用行业/场景
- 核心痛点
- 解决方案概述
- 关联产品数量
- 客户案例数

**解决方案详情页** (`/solutions/:id`):
- 行业背景与挑战
- 解决方案架构图
- 核心功能模块
- 关联产品列表(带链接)
- 客户成功案例
- 实施效果数据
- 咨询按钮

---

### 3.7 资源中心 (Resources)

**路径**: `/resources`

#### 功能点

**资源分类**:
- 产品文档(用户手册、安装指南)
- 白皮书(技术、行业)
- 案例研究(客户成功故事)
- 视频教程(产品演示、操作指南)

**资源列表**:
- 资源标题
- 类型图标
- 文件大小
- 下载次数
- 上传日期
- 下载按钮(需填写表单)

**下载表单**:
- 姓名
- 公司
- 职位
- 邮箱
- 电话
- 用途说明

---

### 3.8 关于我们 (About)

**路径**: `/about`

#### 功能点

**公司简介**:
- 公司使命、愿景、价值观
- 发展历程(时间线)
- 核心团队

**联系我们**:
- 地址、电话、邮箱
- 在线地图
- 联系表单

**新闻动态**:
- 公司新闻
- 产品更新
- 行业洞察

---

## 四、数据模型设计

### 4.1 扩展现有Product模型

```python
# apps/products/models.py

class Product(models.Model):
    # 现有字段...
    
    # === 新增门户相关字段 ===
    
    # 门户展示相关
    is_featured = models.BooleanField(
        default=False, 
        help_text='是否推荐产品(门户首页展示)'
    )
    is_on_portal = models.BooleanField(
        default=True,
        help_text='是否在门户展示'
    )
    sort_weight = models.IntegerField(
        default=0, 
        help_text='排序权重(值越大越靠前)'
    )
    thumbnail = models.ImageField(
        upload_to='product_thumbnails/%Y/%m/',
        blank=True,
        help_text='产品缩略图(建议尺寸:400x300)'
    )
    banner_image = models.ImageField(
        upload_to='product_banners/%Y/%m/',
        blank=True,
        help_text='产品横幅图(建议尺寸:1200x400)'
    )
    
    # SEO相关
    seo_title = models.CharField(
        max_length=200,
        blank=True,
        help_text='SEO标题(浏览器标题栏)'
    )
    seo_description = models.TextField(
        blank=True,
        help_text='SEO描述(搜索引擎摘要)'
    )
    seo_keywords = models.CharField(
        max_length=500,
        blank=True,
        help_text='SEO关键词(逗号分隔)'
    )
    
    # 营销相关
    tagline = models.CharField(
        max_length=200,
        blank=True,
        help_text='产品标语(简短卖点)'
    )
    key_benefits = models.JSONField(
        default=list,
        blank=True,
        help_text='核心价值点(JSON数组)'
    )
    target_industries = models.JSONField(
        default=list,
        blank=True,
        help_text='目标行业(JSON数组)'
    )
    
    # 统计相关
    view_count = models.PositiveIntegerField(
        default=0,
        help_text='浏览次数'
    )
    download_count = models.PositiveIntegerField(
        default=0,
        help_text='资料下载次数'
    )
    
    # 时间相关
    portal_published_at = models.DateTimeField(
        null=True,
        blank=True,
        help_text='门户上线时间'
    )
    
    class Meta:
        indexes = [
            # 新增门户优化索引
            models.Index(
                fields=['is_on_portal', 'is_active', 'sort_weight', '-created_at'],
                name='idx_portal_display'
            ),
            models.Index(
                fields=['is_featured', 'is_on_portal', '-portal_published_at'],
                name='idx_featured_products'
            ),
            models.Index(
                fields=['subsystem_type', 'is_on_portal', 'sort_weight'],
                name='idx_subsystem_display'
            ),
        ]
```

### 4.2 新增Solution模型

```python
# apps/portal/models.py

class Solution(models.Model):
    """解决方案模型"""
    
    SOLUTION_TYPE_CHOICES = [
        ('industry', '按行业'),
        ('scenario', '按场景'),
        ('use_case', '按用例'),
    ]
    
    name = models.CharField(max_length=200, db_index=True)
    solution_type = models.CharField(
        max_length=20,
        choices=SOLUTION_TYPE_CHOICES,
        default='industry'
    )
    category = models.CharField(max_length=100, db_index=True)
    
    # 关联产品(多对多)
    products = models.ManyToManyField(
        'products.Product',
        related_name='solutions',
        blank=True
    )
    
    # 内容
    summary = models.TextField(help_text='解决方案概述')
    pain_points = models.JSONField(
        default=list,
        help_text='核心痛点(JSON数组)'
    )
    architecture = models.TextField(
        blank=True,
        help_text='架构描述'
    )
    architecture_image = models.ImageField(
        upload_to='solution_architectures/%Y/%m/',
        blank=True
    )
    benefits = models.JSONField(
        default=list,
        help_text='实施收益(JSON数组)'
    )
    
    # 案例
    case_study_title = models.CharField(max_length=200, blank=True)
    case_study_content = models.TextField(blank=True)
    case_study_results = models.JSONField(
        default=dict,
        help_text='实施效果数据(JSON)'
    )
    
    # SEO
    seo_title = models.CharField(max_length=200, blank=True)
    seo_description = models.TextField(blank=True)
    
    # 状态
    is_active = models.BooleanField(default=True)
    is_featured = models.BooleanField(default=False)
    sort_weight = models.IntegerField(default=0)
    published_at = models.DateTimeField(null=True, blank=True)
    
    # 统计
    view_count = models.PositiveIntegerField(default=0)
    
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        db_table = 'portal_solutions'
        verbose_name = 'Solution'
        verbose_name_plural = 'Solutions'
        ordering = ['-sort_weight', '-published_at']
        indexes = [
            models.Index(
                fields=['solution_type', 'category', 'is_active'],
                name='idx_solution_filter'
            ),
            models.Index(
                fields=['is_featured', 'is_active', '-published_at'],
                name='idx_featured_solutions'
            ),
        ]
```

### 4.3 新增Resource模型

```python
class Resource(models.Model):
    """资源中心模型"""
    
    RESOURCE_TYPE_CHOICES = [
        ('document', '产品文档'),
        ('whitepaper', '白皮书'),
        ('case_study', '案例研究'),
        ('video', '视频教程'),
        ('datasheet', '数据手册'),
    ]
    
    title = models.CharField(max_length=200, db_index=True)
    resource_type = models.CharField(
        max_length=20,
        choices=RESOURCE_TYPE_CHOICES
    )
    
    # 关联产品(可选)
    product = models.ForeignKey(
        'products.Product',
        on_delete=models.CASCADE,
        related_name='resources',
        null=True,
        blank=True
    )
    
    # 文件
    file = models.FileField(
        upload_to='resources/%Y/%m/',
        max_length=500
    )
    file_size = models.PositiveIntegerField(
        default=0,
        help_text='文件大小(字节)'
    )
    
    # 描述
    description = models.TextField(blank=True)
    excerpt = models.CharField(
        max_length=300,
        blank=True,
        help_text='摘要(用于列表展示)'
    )
    
    # 封面图(视频或文档)
    cover_image = models.ImageField(
        upload_to='resource_covers/%Y/%m/',
        blank=True
    )
    duration = models.CharField(
        max_length=20,
        blank=True,
        help_text='视频时长(如:12:34)'
    )
    
    # SEO
    seo_title = models.CharField(max_length=200, blank=True)
    seo_description = models.TextField(blank=True)
    
    # 统计
    download_count = models.PositiveIntegerField(default=0)
    
    # 状态
    is_active = models.BooleanField(default=True)
    published_at = models.DateTimeField(null=True, blank=True)
    
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        db_table = 'portal_resources'
        verbose_name = 'Resource'
        verbose_name_plural = 'Resources'
        ordering = ['-published_at', '-created_at']
        indexes = [
            models.Index(
                fields=['resource_type', 'is_active', '-published_at'],
                name='idx_resource_filter'
            ),
            models.Index(
                fields=['product', 'resource_type', 'is_active'],
                name='idx_product_resources'
            ),
        ]
```

### 4.4 新增PortalViewLog模型

```python
class PortalViewLog(models.Model):
    """门户访问日志"""
    
    CONTENT_TYPE_CHOICES = [
        ('product', '产品'),
        ('solution', '解决方案'),
        ('resource', '资源'),
    ]
    
    content_type = models.CharField(max_length=20, choices=CONTENT_TYPE_CHOICES)
    object_id = models.UUIDField()
    
    # 访问信息
    ip_address = models.GenericIPAddressField()
    user_agent = models.TextField(blank=True)
    referer = models.URLField(max_length=500, blank=True)
    
    # 地理位置(可选)
    country = models.CharField(max_length=100, blank=True)
    region = models.CharField(max_length=100, blank=True)
    city = models.CharField(max_length=100, blank=True)
    
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        db_table = 'portal_view_logs'
        verbose_name = 'Portal View Log'
        verbose_name_plural = 'Portal View Logs'
        ordering = ['-created_at']
        indexes = [
            models.Index(
                fields=['content_type', 'object_id', '-created_at'],
                name='idx_view_log_content'
            ),
            models.Index(
                fields=['ip_address', '-created_at'],
                name='idx_view_log_ip'
            ),
            models.Index(
                fields=['created_at'],
                name='idx_view_log_date'
            ),
        ]
```

---

## 五、API设计

### 5.1 门户产品API

```python
# apps/portal/views.py

class PortalProductViewSet(viewsets.ReadOnlyModelViewSet):
    """
    门户产品API(只读)
    """
    queryset = Product.objects.filter(is_active=True, is_on_portal=True)
    serializer_class = PortalProductSerializer
    filter_backends = [DjangoFilterBackend, filters.SearchFilter, filters.OrderingFilter]
    
    # 筛选字段
    filterset_fields = {
        'subsystem_type': ['exact', 'in'],
        'category': ['exact', 'in'],
        'vendor': ['exact', 'in'],
        'is_featured': ['exact'],
    }
    
    # 搜索字段
    search_fields = [
        'name',
        'description',
        'tagline',
        'vendor',
        'features__feature_name',
        'features__description',
    ]
    
    # 排序字段
    ordering_fields = [
        'created_at',
        'name',
        'sort_weight',
        'view_count',
        'portal_published_at',
    ]
    ordering = ['-sort_weight', '-portal_published_at']
    
    def get_serializer_class(self):
        if self.action == 'retrieve':
            return PortalProductDetailSerializer
        return super().get_serializer_class()
    
    def retrieve(self, request, *args, **kwargs):
        """获取产品详情,并增加浏览次数"""
        instance = self.get_object()
        
        # 增加浏览次数
        instance.view_count += 1
        instance.save(update_fields=['view_count'])
        
        # 记录访问日志
        self._log_view(instance)
        
        serializer = self.get_serializer(instance)
        return Response(serializer.data)
    
    @action(detail=False, methods=['get'])
    def featured(self, request):
        """获取推荐产品"""
        featured_products = self.get_queryset().filter(
            is_featured=True
        )[:8]
        
        serializer = self.get_serializer(featured_products, many=True)
        return Response(serializer.data)
    
    @action(detail=False, methods=['post'])
    def compare(self, request):
        """
        产品对比
        POST /api/v1/portal/products/compare/
        Body: { product_ids: [uuid1, uuid2, uuid3] }
        """
        product_ids = request.data.get('product_ids', [])
        
        if len(product_ids) < 2:
            return Response(
                {'error': '至少需要选择2个产品进行对比'},
                status=status.HTTP_400_BAD_REQUEST
            )
        
        if len(product_ids) > 4:
            return Response(
                {'error': '最多只能选择4个产品进行对比'},
                status=status.HTTP_400_BAD_REQUEST
            )
        
        products = self.get_queryset().filter(id__in=product_ids)
        
        if products.count() < 2:
            return Response(
                {'error': '有效产品数量不足2个'},
                status=status.HTTP_400_BAD_REQUEST
            )
        
        # 使用特殊序列化器进行对比
        from .serializers import PortalProductCompareSerializer
        serializer = PortalProductCompareSerializer(products, many=True)
        
        return Response({
            'count': products.count(),
            'products': serializer.data
        })
    
    @action(detail=False, methods=['get'])
    def statistics(self, request):
        """获取产品统计信息"""
        queryset = self.get_queryset()
        
        stats = {
            'total_products': queryset.count(),
            'total_features': sum(p.features.filter(is_active=True).count() for p in queryset),
            'subsystem_types': queryset.values('subsystem_type').annotate(
                count=models.Count('id')
            ),
            'categories': queryset.values('category').annotate(
                count=models.Count('id')
            ),
            'vendors': queryset.values('vendor').annotate(
                count=models.Count('id')
            ),
        }
        
        return Response(stats)
```

### 5.2 解决方案API

```python
class PortalSolutionViewSet(viewsets.ReadOnlyModelViewSet):
    """
    解决方案API(只读)
    """
    queryset = Solution.objects.filter(is_active=True)
    serializer_class = PortalSolutionSerializer
    filter_backends = [DjangoFilterBackend, filters.SearchFilter, filters.OrderingFilter]
    
    filterset_fields = {
        'solution_type': ['exact', 'in'],
        'category': ['exact', 'in'],
        'is_featured': ['exact'],
    }
    
    search_fields = ['name', 'summary', 'pain_points']
    
    ordering_fields = ['sort_weight', 'published_at', 'created_at']
    ordering = ['-sort_weight', '-published_at']
    
    def retrieve(self, request, *args, **kwargs):
        """获取详情并增加浏览次数"""
        instance = self.get_object()
        instance.view_count += 1
        instance.save(update_fields=['view_count'])
        
        return super().retrieve(request, *args, **kwargs)
    
    @action(detail=False, methods=['get'])
    def featured(self, request):
        """获取推荐解决方案"""
        featured = self.get_queryset().filter(
            is_featured=True
        )[:6]
        
        serializer = self.get_serializer(featured, many=True)
        return Response(serializer.data)
```

### 5.3 资源下载API

```python
class PortalResourceViewSet(viewsets.ReadOnlyModelViewSet):
    """
    资源中心API(只读)
    """
    queryset = Resource.objects.filter(is_active=True)
    serializer_class = PortalResourceSerializer
    filter_backends = [DjangoFilterBackend, filters.SearchFilter, filters.OrderingFilter]
    
    filterset_fields = {
        'resource_type': ['exact', 'in'],
        'product': ['exact'],
    }
    
    search_fields = ['title', 'description', 'excerpt']
    
    ordering_fields = ['published_at', 'created_at', 'download_count']
    ordering = ['-published_at']
    
    @action(detail=True, methods=['post'])
    def download(self, request, pk=None):
        """
        记录下载
        POST /api/v1/portal/resources/{id}/download/
        """
        resource = self.get_object()
        
        # 增加下载次数
        resource.download_count += 1
        resource.save(update_fields=['download_count'])
        
        # 返回文件URL(实际下载通过Nginx X-Accel-Redirect)
        return Response({
            'download_url': request.build_absolute_uri(resource.file.url),
            'file_name': resource.file.name.split('/')[-1],
            'file_size': resource.file_size,
        })
```

---

## 六、前端架构设计

### 6.1 目录结构

```
frontend/src/
├── views/
│   └── portal/
│       ├── Home.vue              # 门户首页
│       ├── ProductList.vue       # 产品列表
│       ├── ProductDetail.vue     # 产品详情
│       ├── ProductCompare.vue    # 产品对比
│       ├── MatchingInput.vue     # 需求输入
│       ├── MatchingResult.vue    # 匹配结果
│       ├── MatchingHistory.vue   # 匹配历史
│       ├── SolutionList.vue      # 解决方案列表
│       ├── SolutionDetail.vue    # 解决方案详情
│       ├── ResourceList.vue      # 资源列表
│       └── About.vue             # 关于我们
│
├── components/portal/
│   ├── ProductCard.vue           # 产品卡片
│   ├── ProductFilter.vue         # 产品筛选器
│   ├── FeatureTree.vue           # 功能树
│   ├── CompareTable.vue          # 对比表格
│   ├── SolutionBanner.vue        # 解决方案横幅
│   ├── ResourceItem.vue          # 资源项
│   ├── MatchVisualization.vue    # 匹配可视化
│   └── FileUpload.vue            # 文件上传
│
├── api/
│   └── portal.ts                 # 门户API
│
├── types/
│   └── portal.ts                 # 门户类型定义
│
├── layouts/
│   └── PortalLayout.vue          # 门户布局
│
├── store/modules/
│   └── portal.ts                 # 门户状态管理
│
├── assets/portal/
│   ├── styles/                   # 门户专用样式
│   └── images/                   # 门户图片
│
└── router/
    └── portal.ts                 # 门户路由
```

### 6.2 路由配置

```typescript
// router/portal.ts
export const portalRoutes = [
  {
    path: '/',
    name: 'PortalHome',
    component: () => import('@/views/portal/Home.vue'),
    meta: { title: '产品门户' }
  },
  {
    path: '/products',
    name: 'ProductList',
    component: () => import('@/views/portal/ProductList.vue'),
    meta: { title: '产品中心' }
  },
  {
    path: '/products/:id',
    name: 'ProductDetail',
    component: () => import('@/views/portal/ProductDetail.vue'),
    meta: { title: '产品详情' }
  },
  {
    path: '/compare',
    name: 'ProductCompare',
    component: () => import('@/views/portal/ProductCompare.vue'),
    meta: { title: '产品对比' }
  },
  {
    path: '/matching',
    name: 'MatchingInput',
    component: () => import('@/views/portal/MatchingInput.vue'),
    meta: { title: '智能匹配' }
  },
  {
    path: '/matching/results/:id',
    name: 'MatchingResult',
    component: () => import('@/views/portal/MatchingResult.vue'),
    meta: { title: '匹配结果' }
  },
  {
    path: '/matching/history',
    name: 'MatchingHistory',
    component: () => import('@/views/portal/MatchingHistory.vue'),
    meta: { title: '匹配历史' }
  },
  {
    path: '/solutions',
    name: 'SolutionList',
    component: () => import('@/views/portal/SolutionList.vue'),
    meta: { title: '解决方案' }
  },
  {
    path: '/solutions/:id',
    name: 'SolutionDetail',
    component: () => import('@/views/portal/SolutionDetail.vue'),
    meta: { title: '解决方案详情' }
  },
  {
    path: '/resources',
    name: 'ResourceList',
    component: () => import('@/views/portal/ResourceList.vue'),
    meta: { title: '资源中心' }
  },
  {
    path: '/about',
    name: 'About',
    component: () => import('@/views/portal/About.vue'),
    meta: { title: '关于我们' }
  }
]
```

### 6.3 状态管理

```typescript
// store/modules/portal.ts
export const usePortalStore = defineStore('portal', () => {
  // 热门产品
  const hotProducts = ref<Product[]>([])
  
  // 产品分类
  const categories = ref<Category[]>([])
  
  // 产品缓存
  const productCache = new Map<string, Product>()
  
  // 加载热门产品
  async function loadHotProducts() {
    const res = await portalAPI.getFeaturedProducts()
    hotProducts.value = res.data
  }
  
  // 获取产品(带缓存)
  async function getProduct(id: string) {
    if (productCache.has(id)) {
      return productCache.get(id)!
    }
    const res = await portalAPI.getProduct(id)
    productCache.set(id, res.data)
    return res.data
  }
  
  return {
    hotProducts,
    categories,
    productCache,
    loadHotProducts,
    getProduct
  }
})
```

---

## 七、性能优化策略

### 7.1 后端优化

**1. 缓存策略**
```python
# 产品列表缓存(5分钟)
@cache_page(60 * 5)
def list(self, request, *args, **kwargs):
    return super().list(request, *args, **kwargs)

# 产品详情缓存(1小时)
@cache_page(60 * 60)
def retrieve(self, request, *args, **kwargs):
    return super().retrieve(request, *args, **kwargs)
```

**2. 数据库优化**
```python
# 使用select_related减少查询次数
products = Product.objects.select_related('category').all()

# 使用prefetch_related优化多对多查询
products = Product.objects.prefetch_related('features').all()
```

**3. 索引优化**
```sql
-- 产品表索引
CREATE INDEX idx_portal_display ON products(is_on_portal, is_active, sort_weight, created_at DESC);
CREATE INDEX idx_featured_products ON products(is_featured, is_on_portal, portal_published_at DESC);
```

### 7.2 前端优化

**1. 代码分割**
```typescript
// 路由懒加载
const ProductDetail = () => import('@/views/portal/ProductDetail.vue')

// 组件懒加载
const MatchVisualization = defineAsyncComponent(() =>
  import('@/components/portal/MatchVisualization.vue')
)
```

**2. 图片懒加载**
```vue
<template>
  <img v-lazy="product.image" :alt="product.name" />
</template>

<script setup>
import { Lazyload } from 'vant'

const lazyPlugin = Lazyload({
  lazyComponent: true,
  loading: '/images/loading.png',
  error: '/images/error.png'
})
</script>
```

**3. 虚拟滚动**
```vue
<template>
  <VirtualList
    :data-key="'id'"
    :data-sources="products"
    :data-component="ProductCard"
    :estimate-size="200"
  />
</template>
```

**4. 数据缓存**
```typescript
// 使用Pinia缓存产品数据
export const usePortalStore = defineStore('portal', {
  state: () => ({
    productCache: new Map(),
  }),
  actions: {
    async getProduct(id: string) {
      if (this.productCache.has(id)) {
        return this.productCache.get(id)
      }
      const product = await portalAPI.getProduct(id)
      this.productCache.set(id, product)
      return product
    },
  },
})
```

---

## 八、安全与权限

### 8.1 访问控制

**1. IP限流**
```python
# settings.py
REST_FRAMEWORK = {
    'DEFAULT_THROTTLE_RATES': {
        'portal': '100/min',
    },
}
```

**2. 敏感信息过滤**
```python
# PortalProductSerializer中排除敏感字段
class Meta:
    exclude = ['spec_metadata', 'internal_notes']
```

**3. 下载权限**
- 资源下载需要填写表单
- 表单数据验证(邮箱、电话格式)
- 下载链接添加过期时间(30分钟)

### 8.2 数据保护

**1. 日志脱敏**
- IP地址部分隐藏(如:192.168.x.x)
- User-Agent只记录浏览器类型

**2. GDPR合规**
- 提供数据导出功能
- 提供数据删除功能
- 明确的隐私政策链接

---

## 九、UI/UX设计建议

### 9.1 设计原则

**色彩方案**:
- 主色调: 科技蓝 (#1890ff)
- 辅助色: 成功绿 (#52c41a)、警告橙 (#faad14)、错误红 (#f5222d)
- 中性色: 标题黑 (#000000d9)、正文灰 (#00000073)、边框灰 (#d9d9d9)
- 背景色: 浅灰 (#f0f2f5)

**字体规范**:
- 中文: 思源黑体(Source Han Sans)
- 英文: Roboto
- 标题: 18-24px, font-weight: 600
- 正文: 14-16px, font-weight: 400
- 辅助文字: 12-14px, font-weight: 400

**间距规范**:
- 基础间距单位: 8px
- 组件内间距: 16px
- 模块间间距: 24px
- 页面边距: 24px

### 9.2 关键页面设计

**1. 门户首页**
- Hero区域占屏幕60%高度
- 渐变背景(蓝到紫)
- 白色文字,对比度强烈
- 输入框圆角8px,阴影效果

**2. 产品列表页**
- 左侧筛选面板(宽度280px)
- 右侧产品网格(响应式列数)
- 产品卡片阴影+圆角
- 悬停效果(上浮+阴影加深)

**3. 产品详情页**
- 顶部产品信息卡片
- Tab页签切换(功能、规格、统计)
- 功能特性树形结构
- 规格参数表格

**4. 匹配结果页**
- 顶部结果概览卡片
- 左侧图表区域
- 右侧产品排名
- 底部详细匹配情况

---

## 十、实施计划

### 第一阶段:基础门户搭建(2周)

**目标**: 完成门户首页和产品中心基本功能

**任务清单**:
- [ ] 创建门户路由结构
- [ ] 开发门户首页(Hero区域、产品推荐)
- [ ] 产品列表页(搜索+筛选)
- [ ] 产品详情页(基本信息+功能特性)
- [ ] 后端API接口开发(产品列表、详情)
- [ ] 数据库索引优化

**交付物**:
- 可访问的门户首页
- 可浏览的产品列表和详情页
- 基础API接口文档

### 第二阶段:智能匹配中心(2周)

**目标**: 完成需求输入和匹配结果展示

**任务清单**:
- [ ] 需求输入页面开发(文本+文件上传)
- [ ] 匹配结果可视化组件(饼图、柱状图)
- [ ] 匹配结果详情页(分类展示)
- [ ] 匹配历史记录功能
- [ ] 报告导出功能(PDF/Excel)
- [ ] 后端匹配算法优化

**交付物**:
- 完整的需求输入到结果展示流程
- 可视化匹配结果页面
- 历史记录管理功能

### 第三阶段:增强功能(1周)

**目标**: 产品对比和解决方案

**任务清单**:
- [ ] 产品对比功能(最多4个产品)
- [ ] 对比报告导出
- [ ] 解决方案列表和详情
- [ ] 资源中心功能
- [ ] 性能优化(缓存、虚拟滚动)
- [ ] 响应式布局适配

**交付物**:
- 产品对比功能
- 解决方案展示页面
- 优化的性能和用户体验

### 第四阶段:测试与部署(3-5天)

**目标**: 系统测试和生产环境部署

**任务清单**:
- [ ] 单元测试和集成测试
- [ ] 性能测试和压力测试
- [ ] 安全测试(XSS、SQL注入)
- [ ] 生产环境配置
- [ ] 域名和SSL证书配置
- [ ] 监控和日志系统部署
- [ ] 用户培训和文档编写

**交付物**:
- 测试报告
- 生产环境部署
- 用户手册

**总计**: 约5-6周

---

## 十一、资源需求

### 11.1 人力资源

| 角色 | 人数 | 职责 |
|------|------|------|
| 后端开发工程师 | 1人 | Django API开发、数据库设计 |
| 前端开发工程师 | 1人 | Vue 3前端开发、UI实现 |
| UI/UX设计师 | 0.5人 | 界面设计、交互设计(可兼职) |
| 测试工程师 | 0.5人 | 测试用例编写、Bug跟踪(可兼职) |
| DevOps工程师 | 0.5人 | 部署、监控配置(可兼职) |

### 11.2 技术资源

| 资源 | 规格 | 用途 |
|------|------|------|
| 开发服务器 | 4核8G内存 | 开发环境 |
| 测试服务器 | 4核8G内存 | 测试环境 |
| 生产服务器 | 8核16G内存 | 生产环境 |
| PostgreSQL | 12+版本 | 主数据库 |
| Redis | 6+版本 | 缓存和队列 |
| CDN | 100GB流量/月 | 静态资源加速 |
| 对象存储 | 50GB空间 | 文件存储 |

---

## 十二、风险评估与应对

### 12.1 技术风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 嵌入向量计算性能瓶颈 | 中 | 高 | 使用缓存+异步任务,优化算法 |
| 数据库查询慢 | 中 | 中 | 添加索引、使用读写分离 |
| 第三方库兼容性问题 | 低 | 中 | 锁定版本号、提前测试 |

### 12.2 项目管理风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 需求变更 | 中 | 中 | 建立变更流程、预留缓冲时间 |
| 人员变动 | 低 | 高 | 代码审查、文档完善、知识共享 |
| 时间延期 | 中 | 中 | 每日站会、周进度评审、风险预警 |

---

## 十三、成功标准

### 13.1 功能指标

- [ ] 所有页面功能正常,无严重Bug
- [ ] API响应时间 < 200ms(P95)
- [ ] 前端页面加载时间 < 3秒(3G网络)
- [ ] 支持主流浏览器(Chrome、Firefox、Safari、Edge)

### 13.2 业务指标

- [ ] 产品信息完整度 > 95%
- [ ] 搜索准确率 > 90%
- [ ] 用户满意度 > 4.5/5.0

### 13.3 技术指标

- [ ] 单元测试覆盖率 > 80%
- [ ] 集成测试覆盖率 > 70%
- [ ] 性能测试通过率 > 95%
- [ ] 安全扫描无高危漏洞

---

## 十四、后续迭代计划

### 14.1 短期(1-2个月)

- [ ] 用户行为分析(热力图、漏斗分析)
- [ ] 智能推荐(基于浏览历史)
- [ ] 多语言支持(英文版)

### 14.2 中期(3-6个月)

- [ ] 客户评价系统
- [ ] 在线客服集成
- [ ] 产品视频展示
- [ ] AR/VR产品演示

### 14.3 长期(6个月以上)

- [ ] 社区论坛
- [ ] 开发者中心
- [ ] 合作伙伴门户
- [ ] 国际化部署

---

**文档版本**: v1.0
**创建日期**: 2026-01-31
**基于文档**: 产品门户规划方案.md + PORTAL_IMPLEMENTATION_PLAN.md
**最后更新**: 2026-01-31

---

**附录: 参考资料**

1. [Vue 3 官方文档](https://v3.vuejs.org/)
2. [Element Plus 官方文档](https://element-plus.org/)
3. [Django REST Framework](https://www.django-rest-framework.org/)
4. [pgvector 文档](https://github.com/pgvector/pgvector)
5. [ECharts 官方文档](https://echarts.apache.org/)
